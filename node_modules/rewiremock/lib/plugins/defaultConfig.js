'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _path = require('path');

var _utils = require('./common/utils');

const rewireMockConfig = (rewiremock, path = process.cwd()) => {
  let resolvedConfigPath = (0, _path.resolve)(path, 'rewiremock.config.js');
  let fileName = (0, _utils.fileExists)(resolvedConfigPath);
  if (fileName) {
    const config = (0, _utils.ensureRequire)(fileName);
    (config.default || config)(rewiremock);
  }
};

exports.default = rewireMockConfig;