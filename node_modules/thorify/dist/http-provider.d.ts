declare class ThorHttpProvider {
    private host;
    private timeout;
    constructor(host: string, timeout?: number);
    sendAsync(payload: any, callback: any): any;
}
export { ThorHttpProvider };
